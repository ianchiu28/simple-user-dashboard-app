#! /app/.heroku/node/bin/node

const {User} = require('../models');

/**
 * Scheduler main function
 */
function main() {
  console.log('Hello');
  User.count().then((count) => {
    console.log(count);
    process.exit();
  });
}

main();
